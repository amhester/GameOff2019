const EVENTS = {
  HOTKEY: 'hotkey',
  SCENE_CHANGE: 'scene:change',
  // FUTURE Sync commands
  SYNC_HP: 'sync:hp',
  SYNC_BOARD_STATE: 'sync:board',
  MOVE: 'move',
};

const ROLES = {
  NORMAL: 'NORMAL',
};

export {
  EVENTS,
  ROLES,
};
